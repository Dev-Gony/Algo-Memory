{
  "name": "algo-memory",
  "version": "1.0.0",
  "description": "에빙하우스 망각곡선 기반 알고리즘 통암기 스케줄러",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && node test/smoke.test.js",
    "catalog": "python3 content/catalog3.py && node build.js",
    "serve": "node build.js && npx --yes http-server dist -p 8080 -c-1"
  },
  "devDependencies": { "jsdom": "^24.0.0" },
  "license": "MIT"
}
